@model List<Shared.DTOs.OrderSummaryDto>

@{
    Layout = null;    
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <title>Orders</title>
    <link rel="stylesheet" href="~/css/Order.css" />
</head>
<body>

    <h2>Orders List</h2>

    <table class="orders-table">
        <thead>
            <tr>
                <th>Created At</th>
                <th>Status</th>
                <th>Order For</th>
                <th>Total Amount</th>
                <th>Payment Method</th>
            </tr>
        </thead>
        <tbody>
            @* @foreach (var order in Model) *@
            @* { *@
            @*     <tr> *@
            @*         <td>@order.CreatedAt.ToString("yyyy-MM-dd")</td> *@
            @*         <td>@order.Status</td> *@
            @*         <td>@order.OrderFor</td> *@
            @*         <td>@order.TotalAmount.ToString("C")</td> *@
            @*         <td>@order.PaymentMethod</td> *@
            @*     </tr> *@
            @* } *@
        </tbody>
    </table>

</body>
</html>
